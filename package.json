{
  "name": "ops-command-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "for f in tests/*_test.mjs; do node \"$f\" || exit 1; done"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
